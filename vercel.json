{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ],
    "installCommand": "npm install",
    "buildCommand": "prisma generate && vite build"
}